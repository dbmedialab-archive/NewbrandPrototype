export declare enum EndpointType {
    URL = "url",
    GOOGLE_ANALYTICS = "ga"
}
export interface Endpoint {
    type: EndpointType;
    value?: any;
}
export interface SenderConfiguration {
    sendInterval: number;
    batchLimit: number;
    batchInterval: number;
    endpoints: Endpoint[];
}
export default function getSenderConfiguration(userAgent?: string): SenderConfiguration;
