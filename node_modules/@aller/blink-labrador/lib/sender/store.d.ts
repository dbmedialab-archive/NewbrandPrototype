declare class Store {
    getId: (el: any) => string;
    data: {
        [key: string]: any;
    };
    constructor(getId: (el: any) => string);
    add(data?: any[]): void;
    update(entry: any): void;
    flush(): void;
    readonly events: any[];
}
export default Store;
