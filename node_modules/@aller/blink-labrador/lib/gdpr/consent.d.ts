/**
 * We receive a list with numbers, e.g. [1, 2, 3, 4, 5], and update
 * our purposes accordingly. If the number is in the list,
 * it means we have permission for that purpose.
 */
export declare function getConsentData(oil: any): {
    [key: number]: boolean;
};
export declare function hasEveryConsent(oil: any): boolean;
export declare function hasAnsweredOil(oil: any): any;
