"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var includes_1 = require("../includes");
describe('arrayIncludes', function () {
    it('should match only element', function () {
        expect(includes_1.arrayIncludes(['yo'], 'yo')).toBe(true);
    });
    it('should match one element of many element', function () {
        expect(includes_1.arrayIncludes(['hey', 'there', '!'], 'there')).toBe(true);
    });
    it('should not match element not in list', function () {
        expect(includes_1.arrayIncludes(['hey', 'there', '!', '?'], 'yo')).toBe(false);
    });
    it('should return `false` for empty collections', function () {
        expect(includes_1.arrayIncludes([], 'hey')).toBe(false);
    });
    /*it('should match `NaN`', function() {
      expect(arrayIncludes([1, NaN, 3], NaN)).toBe(true);
    });*/
    it('should match `-0` as `0`', function () {
        expect(includes_1.arrayIncludes([-0], 0)).toBe(true);
        expect(includes_1.arrayIncludes([0], -0)).toBe(true);
    });
});
describe('stringIncludes', function () {
    it('should match on equal strings', function () {
        expect(includes_1.stringIncludes('hello?', 'hello?')).toBe(true);
    });
    it('should match one element of many element', function () {
        expect(includes_1.stringIncludes('hey there!', 'there')).toBe(true);
    });
    it('should return `false` for equal empty strings', function () {
        expect(includes_1.stringIncludes('', '')).toBe(false);
    });
    it('should return `false` for empty strings', function () {
        expect(includes_1.stringIncludes('', 'hey')).toBe(false);
    });
});
//# sourceMappingURL=includes.test.js.map