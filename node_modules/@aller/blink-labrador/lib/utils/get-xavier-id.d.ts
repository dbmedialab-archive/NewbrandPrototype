import generateUUID from './uuid';
export interface DocumentWithCookie {
    cookie: string;
}
export default function getXavierId(docu?: DocumentWithCookie, nocache?: boolean, now?: Date, getUUID?: typeof generateUUID, cachedId?: string): string;
