/**
 * Figure out if the page is hidden.
 * @returns {Object} Hidden status
 */
export interface HiddenStatus {
    hidden: boolean;
    prefix: string;
}
export default function getHiddenStatus(): HiddenStatus;
