export interface Location {
    origin: string;
    protocol: string;
}
/**
 * Wraps the DOMElement of a plug on the frontpage and extracts metadata.
 */
declare class ArticlePreview {
    domElement: HTMLElement;
    location: Location;
    constructor(domElement: HTMLElement, location?: Location);
    readonly personalizationParametersRequested: string | null;
    readonly personalizationSystemUsed: string | null;
    readonly fullTitle: {
        fullTitle: string;
        titleParts: {
            text: any;
            fontSize: string;
        }[];
    };
    readonly title: string | null;
    readonly url: string;
    readonly nodeId: string | null;
}
export default ArticlePreview;
