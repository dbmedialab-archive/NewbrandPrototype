import { BlinkEvent } from '../types';
import { Page } from '../selectors/get-page-state';
export interface PrepareActiveTimeProps {
    page: Page;
    id: string;
    time?: Date;
}
/**
 * Prepares a active time data selecting the right
 * fields to send to the server
 */
export default function prepareActiveTimeForSending({ page, id, time, }: PrepareActiveTimeProps): BlinkEvent;
export declare function getAllActiveTimeEventsPrepared(page: any, time: Date): BlinkEvent[];
