import { BlinkEvent } from '../types';
import { Page } from '../selectors/get-page-state';
export interface PreparePageLoadInput {
    page: Page;
    url: string;
}
export default function preparePageloadEvent({ page, url, }: PreparePageLoadInput): BlinkEvent;
