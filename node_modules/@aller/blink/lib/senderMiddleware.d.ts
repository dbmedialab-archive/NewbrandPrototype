import { BlinkEvent } from './types';
export default function senderMiddleware(send: (events: BlinkEvent[]) => any, sendDirect: (events: BlinkEvent[]) => any): ({ getState }: {
    getState: any;
}) => (next: any) => (action: any) => any;
