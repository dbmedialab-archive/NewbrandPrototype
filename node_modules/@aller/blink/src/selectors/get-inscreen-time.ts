import { calculateEventTime } from '../utils/event-time';

export default function getInscreenTime(store: any, id: string, now: Date) {
  if (!store.inscreen[id]) {
    return 0;
  }
  return calculateEventTime({
    times: store.inscreen[id],
    screenEvents: store.screen.events,
    now,
  });
}
