export interface Page {
  state: any;
  id: string;
}

export default function getPageState(
  state: any,
  pageId: string = 'default',
): Page {
  const page = state[pageId];
  return { state: page, id: pageId };
}
